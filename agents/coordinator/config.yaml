# Hermes config for: coordinator

model:
  provider: openai-api
  default: gpt-5.6-sol

terminal:
  backend: local

agent:
  max_turns: 80

tool_loop_guardrails:
  hard_stop_enabled: true

skills:
  write_approval: false

memory:
  write_approval: false

# Hermes config for: consolidator
# Invoked by coordinator via delegate_task

model:
  provider: openai-api
  default: gpt-5.6-sol

terminal:
  backend: docker
  docker_image: "nikolaik/python-nodejs:python3.11-nodejs20"

agent:
  max_turns: 60
  tool_loop_guardrails:
    hard_stop_enabled: true

skills:  { write_approval: false }
memory:  { write_approval: false }
